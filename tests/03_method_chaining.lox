// Method calls (chaining)
class Counter {
  init() {
    this.count = 0;
  }

  increment() {
    this.count = this.count + 1;
  }

  getCount() {
    return this.count;
  }
}

var c = Counter();
c.increment();
c.increment();
c.increment();
print c.getCount();
